# Core backend
fastapi
uvicorn
python-dotenv
python-multipart

# Speech and agent pipeline
# nvidia-pipecat requires Python >=3.12 and is installed from source
git+https://github.com/NVIDIA/voice-agent-examples.git
nvidia-riva-client

# Vision-language model / OCR (example: pytesseract, Pillow)
pytesseract
pillow

# Langchain and Redis for agentic memory
langchain
redis
langchain-community

# NeMo Guardrails
nemoguardrails

# Web UI (if using React/Vite, managed separately)

